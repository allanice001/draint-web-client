// import { CLOSE_MESSAGE } from '../../../../constants/redux/global/messages';

// export default function closeMessage(event, reason) {
//   return (dispatch) => {
//     if (reason === 'clickaway') {
//       return;
//     }
//     dispatch({ type: CLOSE_MESSAGE, payload: { message: '', open: false } });
//   };
// }
